diff --git a/flashchips.c b/flashchips.c
index 9d5b22c..c4daecf 100644
--- a/flashchips.c
+++ b/flashchips.c
@@ -3566,6 +3566,44 @@ const struct flashchip flashchips[] = {
 		.voltage	= {2700, 3600},
 	},
 
+	{
+		.vendor		= "Boya/BoHong Microelectronics",
+		.name		= "B.25Q40AS",
+		.bustype	= BUS_SPI,
+		.manufacture_id	= 0xe0, // Due to some reason, we do not get BOYA_BOHONG_ID (0x68)
+		.model_id	= BOYA_BOHONG_B_25Q40AS,
+		.total_size	= 512,
+		.page_size	= 256,
+		.feature_bits	= FEATURE_WRSR_WREN | FEATURE_OTP,
+		.tested		= TEST_UNTESTED,
+		.probe		= PROBE_SPI_RDID,
+		.probe_timing	= TIMING_ZERO,
+		.block_erasers	=
+		{
+			{
+				.eraseblocks = { {4 * 1024, 128} },
+				.block_erase = spi_block_erase_20,
+			}, {
+				.eraseblocks = { {32 * 1024, 16} },
+				.block_erase = spi_block_erase_52,
+			}, {
+				.eraseblocks = { {64 * 1024, 8} },
+				.block_erase = spi_block_erase_d8,
+			}, {
+				.eraseblocks = { {512 * 1024, 1} },
+				.block_erase = spi_block_erase_60,
+			}, {
+				.eraseblocks = { {512 * 1024, 1} },
+				.block_erase = spi_block_erase_c7,
+			}
+		},
+		.printlock	= spi_prettyprint_status_register_at25fs040,
+		.unlock		= spi_disable_blockprotect_at25fs040,
+		.write		= SPI_CHIP_WRITE256,
+		.read		= SPI_CHIP_READ,
+		.voltage	= {2700, 3600},
+	},
+
 	{
 		.vendor		= "Bright",
 		.name		= "BM29F040",
diff --git a/include/flashchips.h b/include/flashchips.h
index 5df42dc..9b5651c 100644
--- a/include/flashchips.h
+++ b/include/flashchips.h
@@ -212,6 +212,7 @@
 #define BOYA_BOHONG_ID		0x68
 #define BOYA_BOHONG_B_25D16A	0x4015
 #define BOYA_BOHONG_B_25Q128AS	0x4018
+#define BOYA_BOHONG_B_25Q40AS	0x4013
 
 /* Bright Microelectronics has the same manufacturer ID as Hyundai... */
 #define BRIGHT_ID		0xAD	/* Bright Microelectronics */
